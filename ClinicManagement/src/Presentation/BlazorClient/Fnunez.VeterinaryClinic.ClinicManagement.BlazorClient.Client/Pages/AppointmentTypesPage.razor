@page "/appointments"
@using Fnunez.VeterinaryClinic.ClinicManagement.BlazorClient.Client.Shared.Components.IconComponent

<h1>Appointments</h1>

@if (AppointmentTypes is null)
{
    <Spinner></Spinner>
    return;
}

<div class="main-action">
    <button class="btn btn-primary" @onclick="@(() => CreateClick())" @onclick:stopPropagation="true">
        <span class="oi oi-plus" aria-hidden="true"></span> Add
    </button>
</div>

<table class="table">
    <thead>
        <tr>
            <th>Id</th>
            <th>Name</th>
            <th>Code</th>
            <th>Duration</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var appointmentType in AppointmentTypes)
        {
            <tr>
                <td>
                    @appointmentType.Id
                </td>
                <td>
                    @if (IsAddOrEdit(@appointmentType.Id))
                    {
                        <input @bind="ToSave.Name" />
                    }
                    else
                    {
                        @appointmentType.Name
                    }
                </td>
                <td>
                    @if (IsAddOrEdit(@appointmentType.Id))
                    {
                        <input @bind="ToSave.Code" />
                    }
                    else
                    {
                        @appointmentType.Code
                    }
                </td>
                <td>
                    @if (IsAddOrEdit(@appointmentType.Id))
                    {
                        <input @bind="ToSave.Duration" />
                    }
                    else
                    {
                        @appointmentType.Duration
                    }
                </td>
                <td class="action-buttons">
                    @if (IsAddOrEdit(@appointmentType.Id))
                    {
                        <button class="btn btn-success me-2" @onclick="@(() => SaveClick())" @onclick:stopPropagation="true">
                            <SaveIcon></SaveIcon>
                            Save
                        </button>
                        <button class="btn btn-light me-2" @onclick="@(() => CancelClick())" @onclick:stopPropagation="true">
                            <CancelIcon></CancelIcon>
                            Cancel
                        </button>
                    }
                    else
                    {
                        <button class="btn btn-primary me-2" @onclick="@(() => EditClick(appointmentType.Id))" @onclick:stopPropagation="true">
                            <EditIcon></EditIcon>
                            Edit
                        </button>

                        <button class="btn btn-danger me-2" @onclick="@(() => DeleteClick(appointmentType.Id))" @onclick:stopPropagation="true">
                            <DeleteIcon></DeleteIcon>
                            Delete
                        </button>
                    }
                </td>
            </tr>
        }
    </tbody>
</table>